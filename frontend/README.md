Frontend for Instagram Discovery Dashboard

Requirements:
- Node.js and npm

Install and run:

```bash
cd frontend
npm install
npm start
```

This will start a development server (create-react-app) that proxies requests to the Flask API.
